singleServerConfig:
  address: "redis://${REDIS_HOST:localhost}:${REDIS_PORT:6379}"
  connectTimeout: 5000
  timeout: 3000
  retryAttempts: 3
  retryInterval: 1500
  connectionPoolSize: 10
  connectionMinimumIdleSize: 2
  database: 0
threads: 2
nettyThreads: 4