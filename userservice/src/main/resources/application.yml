server:
  port: 9501

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: userservice
  datasource:
    url: jdbc:mysql://localhost:3306/librarysystem?useSSL=false&serverTimezone=UTC
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

idp:
  url: http://localhost:8180
  client-id: librarysystem_app
  client-secret: 0m6GzvbRBg4gaIqApjm1o7c0NheWrmeb
  realm: librarysystem